name: Deprecated Customer Portal

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived status
        run: echo 'prohelper_customers is deprecated; deployment is disabled.'
